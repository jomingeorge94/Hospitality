<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android" xmlns:tools="http://schemas.android.com/tools"
    package="com.example.jomin.hospitality" >

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"></uses-permission>


    <application
        android:allowBackup="true"
        android:icon="@mipmap/nhs_timeline"
        tools:replace="android:icon"
        android:label="@string/app_name"
        android:theme="@style/Theme.AppCompat.NoActionBar" >

        <!-- Google Maps Fragment API Key Data -->
        <meta-data
            android:name="com.google.android.maps.v2.API_KEY"
            android:value="AIzaSyDYH1r7M3QD0847WQKuWcUR3AlX-AIkxe4" />

        <meta-data
            android:name="com.google.android.gms.version"
            android:value="@integer/google_play_services_version" />

        <activity
            android:name=".SplashActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>


        <activity
            android:name=".Login"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>

        <activity
            android:name=".MainActivity"
            android:label="@string/app_name" >

        </activity>

        <activity
            android:name=".Appointments"
            android:label="@string/app_name" >
            </activity>

        <activity
            android:name=".Feedback"
            android:label="@string/app_name" >
        </activity>

        <activity
        android:name=".AboutUs"
        android:label="@string/app_name" >
    </activity>

        <activity
            android:name=".UsefulLinks"
            android:label="@string/app_name" >
        </activity>

        <activity
        android:name=".Reminder"
        android:label="@string/app_name" >
        </activity>

        <activity
            android:name=".CallForHelp"
            android:label="@string/app_name" >
        </activity>

        <activity
            android:name=".FindServices"
            android:label="@string/find" />

        <activity
            android:name=".MyMap"
            android:label="@string/map" />



        <activity
            android:name=".Register"
            android:label="@string/sign_up" />

        <activity
            android:name=".ForgotPassword"
            android:label="@string/forgot_password" />

        <activity
            android:name=".Profile"
            android:label="@string/user_profile" />



    </application>

</manifest>
